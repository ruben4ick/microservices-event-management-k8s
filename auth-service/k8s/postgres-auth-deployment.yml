apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-auth
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-auth
  template:
    metadata:
      labels:
        app: postgres-auth
    spec:
      automountServiceAccountToken: false
      containers:
        - name: postgres
          image: postgres:17

          resources:
            requests:
              cpu: "200m"
              memory: "512Mi"
            limits:
              cpu: "1"
              memory: "1Gi"

          ports:
            - containerPort: 5432

          env:
            - name: POSTGRES_USER
              value: "authuser"
            - name: POSTGRES_PASSWORD
              value: "authpass"
            - name: POSTGRES_DB
              value: "authdb"
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-auth
spec:
  ports:
    - port: 5432
  selector:
    app: postgres-auth

